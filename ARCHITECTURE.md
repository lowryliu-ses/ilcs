# ILCS 系统架构

原型 `prototype-final` 定义了领域语言与页面契约，本仓库把它落成可运行系统，并按《实验室平台开发需求文档》扩展为实验室平台。原型里所有「浏览器内假装的判断」都移到了服务端：裁决权在后端，界面只展示结论与被拒原因。

## 一、进程与边界

```
web (React/Vite)  ──HTTP/JSON──▶  api (FastAPI)  ──SQL──▶  DB (PostgreSQL 16)
      ▲                               │  ▲                     │  ▲
      └──── SSE /api/stream ◀─ LISTEN ilcs_events ◀── NOTIFY ──┘  │
                                         │                        │
                                 共享领域模型          executor（独立进程）
                                                     ├ 控制回路：心跳、监控、推进（串行，不碰设备网络）
                                                     ├ 工位回路：探测 / 对账 / 轮询 / 投递（按工位并发）
                                                     └ LISTEN ilcs_queue：新指令、新事件立即唤醒

               migrate（一次性任务）──▶ DB        LIMS / 设备（服务身份）──HTTP──▶ api
```

进程各有独立生命周期，这是刻意的：

- **api** 无状态，可多副本。所有写操作是一个事务边界。启动只校验库版本，**不建表、不补列、不播种**。
- **executor** 与 api 共享 `app/` 领域模型但不共享进程。它同时是设备执行器与工作流推进器：等待节点到期由它唤醒，不靠浏览器请求触发。设备侧卡死或重启不会拖垮业务读写；反之 api 滚动升级时在跑的批次不掉。它启动时同样校验库版本，不匹配就拒绝启动。
- **web** 不含业务判断。能不能下发、能不能续跑、数据能不能进统计，由服务端回答，前端只负责把 `blocked` 原因显示出来。
- **migrate** 是部署步骤，不是运行时组件。见第五节。

## 二、后端分层

`api/app/` 自上而下依赖，禁止反向引用：

| 层 | 目录 | 职责 | 不允许做的事 |
|---|---|---|---|
| 接线 | `main.py` | 校验库版本、注册路由、领域异常 → HTTP | 建表、补列、播种、任何业务判断 |
| 接口 | `api/routers/`、`api/deps.py`、`schemas/` | 认证（人 / 服务）、作用域装配、DTO 校验、幂等键、分页 | 直接查 ORM |
| 服务 | `services/` | 用例编排，一个方法 = 一个事务 | 写 SQL 细节、拼 HTTP |
| 仓储 | `repositories/` | 聚合读写、作用域过滤、时间线查询 | 业务规则 |
| 领域 | `domain/` | 纯函数与纯数据：能力匹配、方法校验、节点类型、依赖图、排程、多批次顺序搜索与可选 CP-SAT 模型、载具转运计划、开跑检查、恢复策略、条件矩阵、库存三量、资质判定、正式统计 | 碰数据库、碰时钟、碰框架 |
| 适配 | `adapters/` | 设备侧契约、模拟实现、`http_json_v1` HTTPS 网关驱动与 `sila2_v1` 驱动 | 业务状态迁移 |
| 模型 | `models/` | SQLAlchemy 表定义 | 行为 |
| 基础 | `core/` | 配置、会话、`Quantity` 类型、版本校验、访问上下文、时钟、随机数、口令与 JWT、领域异常 | 领域概念 |

`domain/` 是这套架构的价值所在：它不导入 SQLAlchemy 也不导入 FastAPI，因此排程、恢复、库存、统计这些最容易出错的规则可以用普通函数单测，不需要起数据库。`tests/domain/` 就是这样跑的。

领域异常在 `core/errors.py` 定义，每个都带稳定的 `code` 与状态码，路由不写 `HTTPException`：

| 异常 | HTTP | 语义 |
|---|---|---|
| `Unauthenticated` | 401 | 缺少或无效的人 / 服务凭据 |
| `NotFound` | 404 | 对象不存在**或不在本组织作用域内** |
| `PermissionDenied` | 403 | 角色或"不能自审"规则不允许 |
| `StateConflict` | 409 | 状态机或业务规则不允许，`detail` 带结构化原因 |
| `VersionConflict` | 409 | 乐观并发：`row_version` 已变 |
| `IdempotencyConflict` | 409 | 同键不同请求体 |
| `ValidationFailed` | 422 | 输入不满足领域约束 |
| `SignatureInvalid` | 400 | 签名票据无效、过期、已用或绑定的对象版本已变 |
| `ExecutionGateClosed` | 423 | 全局执行门关闭 |

**跨组织一律 404，不是 403。** 403 等于承认"该对象存在"，那本身就是泄露。

## 三、前端分层

`web/src/`：

- `shared/api.ts`：唯一的 fetch 边界，负责 JWT、`Idempotency-Key`、把服务端 `detail.blocked` 解析成可读原因。
  幂等键按「方法 + 路径 + 请求体」缓存：成功或 4xx 后清除，5xx 与网络错误保留，**因此用户重试的是同一个键，不会因为点了两次而下发两次**。
  当前直连验收端口是内网 HTTP（`http://10.10.106.51:8090`），不是安全上下文；正式用户入口仍必须由公共网关提供 HTTPS。所以 `crypto.randomUUID`、
  `navigator.clipboard`、`crypto.subtle` 这些一律不能用——它们在 localhost 上存在、在线上是 undefined，
  本地怎么测都测不出来。幂等键用 `crypto.getRandomValues` 手工拼 UUID v4，且不写「有原生就用原生」的
  分支：那种分支本地永远走一条、线上永远走另一条，等于把出问题的那条路排除在开发期之外。
- `shared/query.ts`：服务端状态层，提供 `useQuery` / `useMutation` 与按前缀（或谓词）失效的缓存。接口刻意对齐 TanStack Query，将来换库只改这一个文件。推送连上时轮询退到 ≥ 60 s 兜底，断开时回到页面声明的间隔；后台标签页不轮询。
- `shared/stream.ts`：变更推送客户端。用 fetch 读 `/api/stream`（原生 `EventSource` 带不了 `Authorization` 头，令牌进 URL 会进访问日志），按主题把对应查询标记失效，300 ms 内的多条合并成一次重取。40 s 没收到任何字节（服务端每 15 s 发保活帧）就判为静默断线、回到轮询并重连——代理或网络断开时连接不一定报错。顶栏显示「实时 / 轮询」。
- `shared/session.tsx`、`shared/signature.tsx`：会话与电子签名。签名票据由服务端签发，绑定动作 + 对象 ID + 对象版本，一次性、两分钟有效。绑定版本是必要的：只绑动作与对象时，对象在签名与提交之间被改过，那张票据仍然能用。
- `shared/ui.tsx`：状态词汇与配色的唯一出处，页面不自己拼颜色。`NumberInput` 在内部保留用户正在敲的原文，避免 `0.` `1e` 这类中间态被 `Number()` 吃掉；`CheckList` 是三态（通过 / 阻塞 / 不适用），因为「不适用」和「通过」在验收里是两件事；`Balances` 把库存三量并排显示，防止只看账面量下结论。
- `shared/chart.tsx`：折线与点图。内联 SVG，颜色走 CSS 令牌，因此浅深色主题一致。不引图表库是权衡：当前图形种类有限，自绘比适配主题省事；遥测点数上量后换 uPlot 只改这一个文件。
- `features/<域>/`：每个页面一个目录，只依赖 `shared/`，互不引用。
- `features/recipes/rules.ts`：方法校验规则在前端的镜像，见下节。
- `features/floor/`：现场总览（中控大屏）。每台工位的健康度、在途指令、放置位上的板；板库槽位；在途转运；位置未知的载具；扫码登记与放置。
- `app/App.tsx`：六组导航、顶栏、路由。导航只列已实现的入口——点进去看到空页面比没有这一项更糟。

## 四、关键机制

**组织作用域**（`core/context.py` + `repositories/base.py` 的 `ScopedRepository`）。每个请求装配出 `AccessContext`（谁、哪个组织、哪些权限），仓储在此之上过滤。**作用域来自成员关系，永不来自请求体里的 `organization_id`**——否则越权只需要改一个字段。系统任务用 `system_context()`，它是显式的、可被审计认出的。

**精确数量**（`core/db.py` 的 `Quantity`）。库存与配料量是 `Numeric(18,6)`，读回来一律是 `Decimal`。不用浮点，也不设"容差"：`0.1 + 0.2 != 0.3` 这种误差在物料台账上会累积成对不上的账，而"差一点就算相等"会把真实的短少掩盖掉。

**库存三量分离**（`domain/inventory.py`）。账面库存、未耗用占用、可用量是三个独立的量，可用量 = 账面 − 占用。台账（`InventoryLedger`）只追加，按「组织 + 来源 + 事件 ID + 行号」唯一，重放不重复记账。**消耗必须由实际投料事件写入，不按步骤比例推算**——推算出来的数字看起来完整，但它不是称出来的。

**六类流程节点**（`domain/steps.py` + `services/workflow_service.py`）。设备 / 人工 / 等待 / 审核，加上系统即时执行的质检关卡与样本拆分（见下文）。每类的适用字段不同，校验只看适用的那些：纯人工流程不会被「没有可承接工位」拦住，没有声明消耗物料的流程不会被「没有 BOM」拦住。`consumes_materials` 默认 `False`——默认要 BOM 就会把合法的空 BOM 流程判成配置错误。

**推进与设备动作分离**。`StepRun` 记录每次尝试，推进走 `WorkflowEvent` 队列 + `StepAdvance` 去重表。设备动作成功不等于流程可以推进，流程推进也不会代替设备确认。指令结果未知时批次转人工核查，**绝不盲目重试**——重试一次不可逆的注液，代价不是一条日志。

推进在保存点里执行：失败只撤销推进本身，同一请求里的人工记录、签名消费与审计保留（幂等边界把 `commit` 收敛成 `flush`，普通 `rollback` 会把它们一起丢掉）。业务性拒绝（状态机不允许、重复事件）直接判 `rejected`；其他错误按指数退避重排（`attempts` + `available_at`），超过 `ILCS_ADVANCE_MAX_ATTEMPTS` 才判失败，并在步骤仍开着时对批次报警。保持或故障中的批次走完最后一步不会被推成「已完成」，恢复评估确认后才结束。

**审核节点驳回**。回到被审核的那一步重做，中间已完成的设备步骤不被跳过也不被重放。这条有回归测试守着：早期实现里驳回只会退一步，隔着一个设备步骤时就退错了地方。

**「采集完成 ≠ 质量有效 ≠ 审核通过」**（`domain/statistics.py`）。三件事分别记录。正式统计只纳入**已审核通过、质量判定有效、且指向一个明确结果版本**的观测；被排除的每一条都带原因进报告的排除说明。`EXCLUSION_REASONS` 是有限集合，报告里出现的是「已审核但质量判定无效」这种可核对的话，不是一句"数据异常"。

**审批与自审**。方案、方法、SOP、报告、数据复核都是「提交人 ≠ 审批人」。矩阵锁定只是不让改，不等于已审批——未审批的方案建不出正式批次。

**执行门**（`domain/gate.py` + `services/gate_service.py`）分两层。公共保护联锁与执行器停止关全站：排程、下发、续跑返回 423，执行器也不投递队列里的动作指令（留在队列，门开后再投）。单台设备失联或心跳超时只进 `blocked_stations`：排程绕开这台设备，用到它的批次下发与续跑 423 并指明设备，其他批次照常——设备一多，任何一台掉线都让全站停摆，等于把单台故障放大成全站事故。**保持与终止不受执行门限制**——门关着时现场恰恰最需要能停下来。公共保护联锁对停用或暂不接受指令的适配器同样生效：它是现场事实，不是设备配置。前端顶栏与横幅同步显示原因。

**保持 / 终止与在途指令**（`services/batch_service.py` + `services/execution_service.py`）。控制动作与执行器投递都先取批次行锁，指令的「领取」与「撤回」是同一个比较并交换（`state=sent AND delivery_state=queued`），二者只有一个成功。保持或终止时还在队列里的动作指令被撤回（`cancelled / not_sent`），设备侧从未见过它；设备侧有在途动作时才发保持 / 终止指令，并以设备回执为准。工位的 `current_command_id` 只属于动作指令，保持 / 终止不覆盖它，否则下一轮对账会把仍在执行的原动作判成不一致。续跑时若本步动作从未送达设备，就按首次下发（`dispatch`），不给设备发一个它没见过的动作的「继续」。

**正式环境禁止模拟**。`ILCS_ENVIRONMENT=production` 时模拟适配器不能执行任何指令（注册表拒绝实例化、下发返回 `simulation_adapter`），模拟心跳被配置门禁拒绝。漏配成模拟的工位不会在没有硬件的情况下报告「执行完成」。

**结果未知只能由人下结论**（`BatchService.verify_command`）。系统不替人猜设备动没动：到现场核实的人给出「已执行 / 未执行 / 部分执行」三选一，写明依据并签名（签名绑定这条指令）。已执行写一个来源为 `manual_verification`、质量为 `uncertain` 的检查点并照常推进；终止指令已执行即现场已安全停机，批次终止——设备离线时终止只能走这条路。未执行的步骤可以重试；部分执行的物理状态不可复现，续跑与重试都被禁止。

**报警分来源、按条件去重**（`services/alarm_service.py` + `services/monitoring_service.py`）。`origin=device` 的报警只接受设备上报恢复；`origin=system` 的报警是软件判定的（执行异常、推进卡住、失联、心跳超时、校准临期、指令超时），设备不知道它们的存在，条件要么由系统判定消除后自动复位，要么由操作员写明原因并签名清除。`condition_key` 保证同一条件持续期间只报一次。复位只动条件，不动确认与关闭。

**执行器存活与指令超时**。执行器每轮写 `executor_heartbeats`；超过 `ILCS_EXECUTOR_STALE_SEC` 没有上报，执行门关闭——否则界面照常下发，却没有进程去投递、去推进。在途指令按步骤预计时长判超时：超过 1.5 倍 + 5 min 报警，超过 3 倍 + 5 min（或能力恢复规则里的 `maxRunMin`）转结果未知、人工核查，不自动重试。执行器日志为一行一条的 JSON；收到 SIGTERM 做完当前一轮再退出；连续失败时指数退避。

**设备回执与轮询同一套结论**（`ExecutionService.settle`）。回执入口与执行器轮询都经由它写检查点、结束指令、释放工位的「当前指令」再交给推进器；只发事件不结束指令，不支持查询的设备会让指令永远停在执行中。

**方法审批的职责分离**。方法记录作者与提交人的稳定用户 ID；批准人不能是二者之一，管理员也不例外。审批 / 发布签名按严格模式核对：票据必须针对该方法及其当前 `row_version`，未写对象的票据不再当作通配。用过的步骤 ID 记在 `used_step_ids`，删掉的也不复用；修订号与修订版本号取已有最大值 + 1；修订版发布即退役来源版本。

**SiLA 2 设备**（`adapters/sila2.py` + `contracts/sila2/TaskExecution.sila.xml`）。`sila2_v1` 驱动按 ILCS 指令号提交、查询、保持、终止任务，回执与 `http_json_v1` 共用 `adapters/contract.py` 的解读规则。SiLA 定义错误（联锁、参数非法、忙、不支持）是明确失败；连接失败、超时是结果未知；未定义执行错误与不合规回执是「有响应但无法确认」，同样按结果未知处理。SiLA 设备不推心跳：执行器按 `probe_interval_sec` 读设备身份判在线并同步联锁；设备自报 `simulator: true` 时正式环境拒绝接入。`simulators/sila_device/` 是系统外部的 SiLA 2 模拟设备，带故障注入，用于真机到位前的联调验收。

**质检关卡与样本拆分**（`domain/steps.py` 的 `gate` / `split` + `WorkflowService.enter`）。两者由系统在步骤开出时立即执行。关卡读取测量来源（它之前的某个设备步骤）最近检查点里的测量值，按阈值判定：整批判定不合格时按方法配置返工（返工目标到关卡之间已完成的步骤标为「已被返工取代」，记录保留，流程从返工目标重做；超过次数转人工）、报废（批次故障、样本失败）或保持待 QA 签名判定；逐孔位判定只剔除不合格样本。取不到数值不等于合格，一律转人工。拆分为每个在用样本登记 N 个子物理样本（`parent_id` 指向母样）与子运行分配，母样运行分配标为已拆分。

**并行通道**（`Station.channels`）。同一工位同一时刻最多承接 `channels` 个批次（如 8 通道充放电柜），排程允许时间窗重叠到这个数；写入前的守门（`ScheduleService._overlaps`）用同一口径按扫描线计数，超出通道数才报重叠——早期实现在这里按「任意两段重叠」判，领域排程算出的合法结果会在写入时被 409 拒绝。对账不再按「工位只有一条在途指令」判定。样品位（`positions`）是单个批次的容量，与并行无关。

**闭环实验**（`services/proposal_service.py`）。方案带设计空间（因子边界、禁止组合、每轮最多点数），随审批冻结。外部优化器用服务身份（`plan_proposals` 授权）或研究员提交提案：设计点逐个核对设计空间、样品位与工位参数范围，拒绝的提案连同原因留档；接受的生成下一轮方案草稿（显式设计点，不做全因子组合），仍需锁定、提交、QA 批准才能建批次。训练数据按实验活动（根方案及其各轮）导出，只含复核通过、质量有效、未被更正版本取代的结果。

**执行器单活**。PostgreSQL 上执行器以会话级 advisory lock 做主备，对账、轮询与领取都按「只有我在处理这些指令」设计；持锁连接断开即退出，由容器重启后重新竞争。

**并发执行器**（`services/executor_runtime.py`）。单活不等于单线程。每轮分两部分：控制回路在主线程里写执行器心跳、模拟心跳、设备监控报警、流程推进，只碰数据库；工位回路进线程池，每个有事要做的工位一个任务（探测、对账、轮询、超时、投递），同一工位同一时刻只有一个任务，工位内指令顺序不变。上一轮的任务没做完，这一轮不给它派新活也不等它，超过 `ILCS_EXECUTOR_STATION_STUCK_SEC` 记告警日志并写进执行器心跳的 `detail`。这样一台设备网关卡住只拖住它自己——原来的串行回路里，几台一起卡就能让一轮超过 60 s、执行器心跳过期、全站执行门关闭。`ILCS_EXECUTOR_MODE=serial` 退回串行回路。线程各用自己的会话；批次行锁、指令比较并交换、步骤行锁与推进去重表本来就为「API 与执行器同时写」设计。

**指令队列只取可投递项**（`CommandRepository.pending`）。未到最早投递时刻的、前置指令未完成的、执行门关闭时的动作指令都不进候选；保持 / 终止排在动作指令前面。否则它们占满 LIMIT，排在后面已到点的指令永远轮不到（队首阻塞），且每条都白取一次批次行锁。`0013` 给队列与在途扫描加了部分索引。

**变更推送**（`core/events.py` + `core/stream_hub.py` + `api/routers/stream.py`）。会话的 `before_commit` 钩子把本事务改过的对象归成主题（批次、指令、报警、工位、执行门、排程、遥测、载具），经 `pg_notify` 与业务写入同事务发出：提交才投递、回滚即丢弃，幂等边界把 `commit` 收敛成 `flush` 时不会提前发。只改心跳时间这类高频字段的更新不推送。`ilcs_queue` 频道唤醒执行器（新指令入队、新推进事件、转运完成），它等在 `select()` 上而不是睡满轮询周期；`ilcs_events` 频道由每个 API 进程一个监听线程分发给本进程的 SSE 订阅者，按组织过滤。**只推「什么变了」，不推数据**：载荷里只有主题与对象 ID，客户端带着自己的令牌重取，权限与作用域照旧。推送连接最长 `ILCS_STREAM_MAX_SEC`，到点由服务端结束、客户端重连并重新鉴权；`X-Accel-Buffering: no` 让 nginx 不缓冲。通知可以丢，正确性从不依赖它。

**载具、位置与可执行的转运**（`models/labware.py` + `domain/labware.py` + `services/transfer_service.py`）。位置（工位放置位、板库槽位、缓冲位）与工位一样是全站共享的物理资源；载具（板、托盘）按组织登记，条码在组织内唯一，一个位置同一时刻只放一块（部分唯一索引）。**位置只由被确认的事实写入**：转运指令的设备回执，或操作员扫码（条码必须与载具一致）；不按计划推算。移位记录只追加。批次绑定载具后，每个设备步骤开始前若板不在目标工位，系统先生成一条 `type=transfer` 的指令投给承运工位（AGV / 机械臂），并把它设为该设备动作的前置指令（`commands.after_command_id`）：转运没被确认完成，设备收不到动作。转运走与设备动作相同的执行门、联锁、比较并交换、超时与结果未知核查；单通道承运工位上一条动作没结论时不塞第二条。转运结果未知时等着它的设备动作撤回；现场核查「已执行」按核查人记录位置，「部分执行」把位置标为未知、须扫码重新定位才能续跑。转运进行中不能保持（板搬到一半停下既不在起点也不在终点），只能等它完成或终止；终止发给承运工位，终止后位置标为未知。下发与恢复在签名前先核对转运计划（位置未知、目标放置位满、没有可用承运工位），不成立就 409 并说明原因。没绑定载具的批次不做位置追踪，行为与之前一致；`ILCS_LABWARE_REQUIRED=1` 时下发要求先绑定。

**依赖图流程**（`domain/graph.py`）。步骤可声明 `after: [step_id…]`。没有任何步骤声明时是顺序流程，走原来的推进路径，行为不变；有声明时进入依赖图模式，未声明的步骤仍依赖上一行，所以只需在分叉、汇合处写。前驱必须排在前面，列表即拓扑序，结构上不可能有环。推进时一步完成开出所有「前驱都已完成且从未尝试过」的步骤——失败、结果未知、进行中的步骤不会因另一个分支完成而被重新开出（那等于盲目重试物理动作）；汇合步骤的开出按「汇合步骤本身」去重，且先取批次行锁再重读步骤实例，两个分支在不同事务里同时完成也只开一次、不会谁都不开；全部步骤完成才结束批次。批次绑定了载具时，并行分支上的两个设备步骤不会同时占用这块板，后到的等前一个结束。排程按依赖图算：每步从最晚结束的前驱接手（时间与载具所在工位）；硬时限从最晚结束的前驱起算。故障时 `current_step` 记为出问题的那一步，恢复评估针对它。编辑器在步骤属性里勾选前驱，画布标出分叉与汇合，检查清单改报关键路径。

**多批次优化**（`domain/optimizer.py`，可选 `domain/cpsat.py`）。排程器对给定批次顺序是确定性的，且保证全部约束，所以优化只搜索顺序：每个候选都交给同一个排程器解码，优化器只比结果（先总跨度，再按优先级加权的完成时间），不另造一套约束。≤ 6 个批次穷举；更多用迭代局部搜索（插入邻域、首次改进、卡住随机扰动，固定随机种子）。装了 OR-Tools 时 CP-SAT 模型（可选工位区间、按通道数的累积约束、依赖图与转运间隔、硬时限、已有占用）给出一个候选顺序与最优性差距一起参与比较；它不含承运与清洗缓冲，写进时间线的时间窗仍由排程器生成。应用优化方案时先清掉所选批次的旧时间窗再按顺序逐个排——早期实现逐个重排时其他所选批次的旧占用还在，写入结果与操作员确认的预览对不上，甚至被重叠守门拒绝。

**开跑检查**（`domain/preflight.py`）。九项检查，每项三态：通过 / 阻塞 / 不适用。`dispatch` 会再算一遍，不信前端传来的结论。

**幂等**（`api/deps.py` + `core/db.py`）。带 `Idempotency-Key` 的写操作把首次结果记在 `IdempotencyKey` 表，键的作用域是「组织 + 主体 + 动作 + 键 + 请求摘要」；重放返回同一业务结果，设备侧不产生第二次动作，同键不同体返回 409。`ManagedSession` 在幂等边界内把领域服务的 `commit` 收敛为 `flush`，业务、审计、签名消费与幂等响应最终一次提交；故障注入证明记录响应前抛错会整项回滚。PostgreSQL 另用一条独立、持续 checkout 的连接持有会话级 advisory lock；不能借 ORM Session 的连接持锁，否则连接归还池后会出现锁重入或无法在原连接解锁。

**服务身份**（`services/identity_service.py`）。设备与 LIMS 用 `X-Service-Source` + `X-Service-Secret`，凭据原文只在签发响应里出现一次，库内只存摘要，日志不记录明文。结果回传与设备事件都要求稳定的 `event_id`。**没有匿名兼容路径**——留一条"暂时先不鉴权"的路，它就会一直在那。

**电子签名**。关键动作要签名票据，票据在服务端消费一次即失效，并写入只追加的审计记录。

**账号口令**（`core/security.py`）。新摘要格式包含算法、迭代次数、每账号 128 位随机盐和摘要；pepper 只在运行配置中。旧库的无盐 PBKDF2 摘要仍可验证，但会在成功登录后立即重算为新格式。生产入口对登录做共享限流，API 容器不映射宿主端口。

**报告发布的固化**（`services/report_service.py` + `report_pdf.py`）。发布时把结果版本清单、算法版本、模板版本、PDF 摘要与签名一起写进快照，之后报告只读。PDF 用 reportlab 内置的 `STSong-Light` CID 字体，不随包字体文件；CID 字体的字形映射不是 Unicode，项目符号只用实测过的 `U+30FB`。

**删除与停用的分界**（`domain/lifecycle.py`）。审计只追加、批次快照只读，所以「删除」不能一刀切。
分界线只有一条：**这条记录有没有被别的记录引用过**。没被引用的草稿是纯粹的编辑残留，删掉不丢信息；
一旦被引用——方法出过批次、批号进过投料、工位排过工步——记录本身就是某段历史的一部分，删了会让审计链、
样品谱系或物料台账出现讲不通的空洞，这时只能停用 / 退役 / 报废。判据是纯函数，返回「不能删的理由」列表，
同一份文案既进 409 的 `detail.blocked`，也回在列表接口的 `delete_blockers` 上给界面禁用按钮并写进 `title`，
所以用户看到的是「已被 3 个批次引用：B-1、B-2」而不是一句「操作失败」。

| 对象 | 可删的条件 | 否则 |
|---|---|---|
| 方法配方 | 草稿，且无批次 / 方案引用、无派生修订 | 按状态给建议：评审中先撤回、已发布走退役 |
| 实验方案 | 草稿，且无批次绑定 | 已锁定先解锁 |
| 批次 | 未下发（planned / scheduled）且从未发过指令 | 已下发走终止；删除时解绑实验任务 |
| 能力 | 无工位实现且无方法引用 | 停用：新步骤不能再选，已有方法与快照不受影响 |
| 批号 | 从未被预留 | 报废：库存清零、不可再预留，记录留档 |
| 工位 / 设备 | 永不删除 | 停用 / 退役：退出排程匹配，历史分配仍指向它 |
| 人员资质 | 从未用于分配 | 到期：不再满足分配判据，记录留档 |
| 废液桶 | 液位为 0 | 先换桶清空 |
| 报警 / 结果 / 报告 / 审计 | 不提供删除 | — |

停用不是打个标记就完：`StationSpec.retired` 让工位退出 `stations_for_step` 的匹配，
停用的能力在 `step_issues` 里变成一条阻塞项，报废批号从 `released_for` 与方案的物料预览里消失——
否则「停用」只是界面上的字，排程照排、方法照过。

**唯一约束只约束"活着"的行**。`experiment_tasks.batch_id`、`slot_occupancies` 这类列上用的是带 `WHERE` 的部分唯一索引，不是整列唯一。整列唯一要么把空串 / NULL 也算进去而过度约束（一个任务还没绑批次就和另一个冲突），要么因为 NULL 互不相等而漏掉真正的重复（同一工位同一时段被占两次）。

**方法校验的两处实现**（`domain/recipe_rules.py` 与 `web/src/features/recipes/rules.ts`）。图形化编辑器需要逐键反馈，不可能每次都往返服务端，所以同一套判据写了两遍，且都按节点类型分支。前端那份只负责提示，`submit` 由服务端重算裁决，因此两边不一致时表现为「界面说通过、提交被 409 拒绝」——改规则必须同时改两处，`tests/domain/test_recipe_rules.py` 守着服务端那份。

**检查点与重启对账**（`services/execution_service.py`）。每步完成写唯一检查点；执行器重启后逐条比对设备侧的 `current_command_id`，不一致就把批次挂起报警，绝不自动重试。

**终止的三条路径**。设备侧有在途或结果未知的动作时，终止需要设备确认（`aborting` → 设备回执 → `aborted`，在途动作指令随之结束）；已下发但设备侧没有在途动作（首条动作还在队列、当前是人工 / 等待 / 审核节点）时，撤回队列指令并直接终止；从未下发的批次直接终止并立刻归还工位时间窗与物料预留。三者都有回归测试守着。

**排程写入串行化**（`services/schedule_service.py` 的 `lock_schedule`）。排程先读全站占用再写新占用，两个请求同时排会读到同一份时间线。排程、重排、优化写入与恢复后移都先取事务级 advisory lock 并在锁内重读时间线，写入前再核对一次与其他批次无重叠。硬时限在转运把开工时间往后推之后再判。

**按时开工**（`ScheduleService.realign` + `Command.not_before`）。设备动作指令带最早投递时刻（时间窗开始减 15 分钟），执行器到点前不投递；开跑检查拦下提前 15 分钟以上的下发。每发一条动作指令先按实际进度对齐本批剩余时间窗：上游提前完成且提前不与别的批次冲突就整体提前，否则等原时间窗；晚开工整体顺延，与别的批次重叠只报警、不自动挤占。设备提前完成时把没用完的时间窗和清洗缓冲还给排程。

**排程的资产约束**。维护、校准预约占满整台资产，人工预约占一份，映射到同一资产的多个工位共享它的容量；资产处于维护或退役、设备失联或心跳超时的工位不承接排程。

**矩阵条件下发**。因子可声明 `target: {step_id, param}`。方案校验逐个水平核对是否落在可承接工位的参数范围内；建批次时按孔位冻结进 `plan_snapshot.condition_params`，该步骤的设备指令在 `params.wells` 里带全部孔位的参数，步骤固定参数是缺省值。

**设备回报的实际消耗**（`services/consumption_service.py`）。回执 `delivered.materials` 里的每一项按「指令号#序号」去重写成库存消耗事件，按批号或物料名对到本批次预留。超出预留、对不上预留或单位换算不了一律不入账并报警；与计划量（BOM 按消耗步骤均分，只作对照）偏差超过阈值照常入账、报警待复核——账上记的永远是设备称出来的量。

**维护工单**（`services/maintenance_service.py`）。建单即登记维护占用（排程让路），开工资产转入维护状态（开跑检查拦截），完工写记录并签名；合格恢复原状态，不合格保持维护状态，取消恢复开工前状态。

**校准许可以最近一次为准**（`domain/resources.py` 的 `governing_calibration`）。步骤开始前最近生效的一条校准决定许可：后登记的不合格压过更早的合格；执行区间内登记了不合格同样阻塞；合格校准必须填有效期，没有有效期的旧记录不作为许可依据。资产处于维护状态、工位离线时不参与排程与开跑。

## 五、迁移

`api/alembic/versions/`：

| 版本 | 内容 |
|---|---|
| `0001_legacy_baseline` | 现有结构基线。手写并与生产库逐表核对过，用于给老库打标记 |
| `0002_platform` | 平台结构：新表、新列（全部带 `server_default`）、部分唯一索引 |
| `0003_history_mapping` | 历史数据映射：固定指标 → 受版本控制的指标、结果 → 类型化结果（`review_state=pending`、来源标记 `legacy_unreviewed`）、样本 → 物理样本 + 运行分配、方法步骤补 `kind`、组织归属 |
| `0004`–`0007` | 适配器配置、样本关联、服务身份并发版本、账号生命周期 |
| `0008_event_retry` | 推进事件重试计数 `workflow_events.attempts`（已有事件记为 0） |
| `0011_automation_campaigns` | 工位并行通道 `channels`（已有工位记为 1）、方案设计点 / 设计空间 / 来源方案 / 轮次、提案表 |
| `0010_operations_b` | 指令最早投递时刻 `commands.not_before`、遥测 `event_id` 与部分唯一索引、维护工单表 |
| `0009_operations_hardening` | 方法作者 / 提交人 / 已用步骤 ID / 乐观锁，工位乐观锁，指令开始与超时时刻，报警来源与去重键，执行器存活表。历史方法作者留空（不伪造成真人），批次报警回填为 `system` 来源 |
| `0012_role_permissions` | 账号多角色、组织可编辑的角色权限矩阵 |
| `0013_queue_indexes` | 指令队列部分索引、指令状态索引、遥测按批次与设备时间的索引 |
| `0014_executor_detail` | 执行器存活记录的每轮运行情况（耗时、线程数、仍在跑与疑似卡住的工位） |
| `0015_labware_locations` | 载具类型、位置、载具、移位记录四张表；指令的前置指令与所搬载具两列。不预置任何位置：没有登记位置的部署不启用位置追踪 |

规则：

- **应用启动只校验版本**（`core/schema.py` 的 `EXPECTED_REVISION`），不一致就拒绝进入可用状态。`core/migrate.py`（原来那个只支持加列的权宜实现）已删除。
- 迁移由 `scripts/migrate.py` 独立执行。`upgrade --stamp-baseline` 对**有老表但没有版本记录**的库打基线标记；空库直接从头建，不打标记（打了就再也不会建基线里的表）。
- 结构可逆不等于业务动作可逆：`0003` 的 `downgrade()` 直接抛错。它把历史结果标成"未复核"，回退无法恢复"当初到底审过没有"——那个答案从来就不在库里。
- 迁移不替业务做决定：在途批次、结果未知的指令、按步骤比例推算过的消耗量，都留在 `migrate.py verify` 的待确认清单里，由责任人处理（DEC-05）。
- 迁移报告落在 `docs/migration-report.md` 与部署卷的 `/data/migration-report.md`。

## 六、验证

```bash
cd ilcs/api && .venv/bin/pytest -q          # 领域单测 + API 集成测试
cd ilcs && api/.venv/bin/python scripts/smoke.py   # 端到端闭环（需 api 与 executor 在跑）
```

测试用真实的 alembic 链建库（`tests/conftest.py`），不是 `create_all`：这样迁移本身也在每次跑测试时被验证一遍，避免"模型能建、迁移建不出"。

`scripts/smoke.py` 走首期完整链路：方案审批 → 任务分配与接单 → 批次与排程 → 开跑检查与签名下发 → 人工记录 → 设备执行 → 等待计时 → 流程审核 → 检测任务与服务认证回传 → 数据复核 → 报告审签与发布 PDF。它只用 HTTP，因此也能对着部署环境跑。

## 七、按方案演进时要换的地方

| 现状 | 目标 | 影响面 |
|---|---|---|
| 数据库指令队列 + LISTEN/NOTIFY 唤醒 | NATS JetStream（多执行器分片、跨站点） | `repositories/execution.py`、`core/events.py` 与执行器循环 |
| JWT + 本地口令 | Keycloak OIDC | `core/security.py`、`api/deps.py`；服务身份不变 |
| 模拟适配器 | 厂商专用工位适配器 | 已内置 `http_json_v1` HTTPS 网关驱动；不能适配统一网关的设备再实现 `adapters/base.py` 契约并在 `adapters/registry.py` 注册。**待 DEC-02 定下首台设备协议、能力与超时语义**；未注册驱动在健康检查、重连和执行时明确拒绝 |
| 模拟遥测序列与模拟原始曲线 | TimescaleDB 连续聚合 / 对象存储 | `ExecutionService.record_telemetry`、`ResultService.raw_curve_rows`；读接口不变 |
| 本地文件存储 | 对象存储 | `services/file_service.py` 一处；接口返回的是文件 ID，不是路径 |
| 自绘 SVG 图表 | uPlot | `web/src/shared/chart.tsx` |
| 贪心解码 + 顺序搜索；CP-SAT 可选（装 ortools 即启用） | CP-SAT 直接给出时间窗 | `domain/cpsat.py`、`ScheduleService.optimize_preview`。**ortools 9.15 要求 protobuf < 7，与 SiLA 2 驱动所用 grpcio-tools 1.84（protobuf ≥ 7.35）冲突**；启用前需确定 gRPC 栈版本并回归 SiLA 用例 |
| 承运工位走 `http_json_v1` / 专用驱动接收转运指令 | 车队系统（Open-RMF 等）对接 | 转运指令契约（`params.from/to/labware_id`）不变，新增驱动在 `adapters/registry.py` 注册 |
| 无外部集成 | LIMS / ERP 双向 | 现有服务身份与 `event_id` 幂等已是入向契约；出向需新增 `services/integration_service.py` |

每一行都限定在一两个文件里，这是分层的直接收益。
