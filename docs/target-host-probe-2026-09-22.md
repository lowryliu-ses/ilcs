# 目标机只读探测记录

- 日期：2026-09-22（Asia/Shanghai）
- 目标：`10.10.106.51:22`
- SSH 配置解析用户：`root`
- 操作性质：只读；计划查看 ILCS 容器、`/opt/ilcs` 文件和 Compose 状态
- 结果：连接在 10 秒内超时，未进入 SSH 认证阶段
- 变更：无；未上传文件、未执行远端命令、未停止服务、未修改数据库

## 结论

当前工作环境到目标机的网络路径不可达，因此不能证明目标机上运行的是旧原型还是当前 ILCS
栈，也不能执行 AC-40 的目标机首次部署与恢复演练。该结果不是应用、账号或部署脚本失败。

恢复条件：接入能够访问该网段的 VPN/跳板机或在目标机所在网络运行部署命令，并取得明确的
上线变更授权。恢复后先重复只读检查，再按 [上线前输入与授权清单](上线前输入与授权清单.md)
选择数据路径并执行，不得根据本次超时推断远端可以覆盖。
